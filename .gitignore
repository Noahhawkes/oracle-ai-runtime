# Logs
Logs/
*.log

# Local secrets
.env
*.key
*.pem

# OS/system files
.DS_Store
Thumbs.db
desktop.ini

# Node/Python artifacts
node_modules/
__pycache__/
*.pyc

# Runtime data
Memory/
*.db

# Approval gate runtime output — live pending candidate data, may contain
# sensitive rendered context awaiting human approval. Must stay local.
Projects/pending_candidates/

# Daemon runtime proposals — generated output, not source. Stays local.
Projects/daemon_proposals/
Oracle_Daemon_Proposal.md
goals.txt
core/Desktop/

# OAuth credentials and tokens — must never be committed
storage/token.json
storage/credentials.json
storage/*.json

# OBS Studio capture profiles/configs — runtime junk, and profile .ini files
# can embed live OAuth tokens. Never commit.
data/obs_captures/

# User identity and personal documents (privacy — reviewed separately)
Users/

# PyInstaller build artifacts
dist/
build/
*.spec.bak

# Temporary files
*.tmp
*.bak

# Raw thread exports — never commit raw conversation text
thread_exports/

# Local AI bridge/message logs - runtime coordination, not source
Messages/*.md

# Raise-Hand Protocol runtime queue and ledger
state/
.snapshots/
Projects/patch_proposals/
.aider*

# generated drive scan report (regenerable)
oracle_content_scan.txt

# Live-coding backup snapshots + scratch scan output (not source, regenerable)
*.bak_*
*_hits.txt
*_brief.txt
*_inspection.txt
*_diff.txt
*_uncommitted.patch
observation_boss_lair.txt
vision_bridge_inventory.txt
approval-continuation.patch
patch_approval_continuation*.py
fallback_prompt_inspection.txt
stream_reply_inspection.txt
oracle_server_diff.txt
oracle_ingest_test/.pytest_cache/
.claude/
_preservation_checkpoints/
sandbox/
sandbox.trash/

# Intake runtime staging (large, regenerable) + local backups
intake/staging/
Backups/

# Generated thread-pass ingestion receipts (regenerable via seed_loader)
data/receipts/

# Staged large build directives (local-only user content, may be large/personal)
data/build_directives/

# RedInk ingestion test workspace (scratch, recreated per test)
noah_exocortex/

# RedInk sovereign custody store (local-only, not committed)
custody/
