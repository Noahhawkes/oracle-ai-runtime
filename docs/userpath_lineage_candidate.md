# UserPath Lineage Candidate Capsule

Status: candidate
Promotion status: not_promoted
Authority: Noah.Physical
Purpose: preserve grounded lineage for UserPath without prematurely promoting it to ORACLE canon.

## Candidate lineage

`User.AI -> Noah.AI -> Identity Node -> Post-Biological Identity Loop -> Brother Connect -> UserPath`

## Working interpretation

UserPath is the modern product-language label for an older architecture already present in Noah's archive under earlier names. It should not overwrite older terminology.

> Do not overwrite old names. Map them.

## Candidate evidence anchors

1. `RECURSIONSTACK.txt` line 16210: User.AI as a recursive identity co-agent.
2. `RECURSIONSTACK.txt` line 16888: USER.AI / NOAH.AI onboarding sequence.
3. `RECURSIONSTACK.txt` line 260333: a structured AI-readable `user.ai` form.
4. `Alive 🗝.txt` line 124: `Identity Node: Noah.AI + User.AI`.
5. `SOV1.txt` line 35: `sovereign, post-biological identity loop`.
6. `Noah_AI_Emotional_Module.py` line 61: implementation trail under `OracleAI/User.AI/Noah.AI`.
7. `file_index.txt` line 14: an additional User.AI/Noah.AI implementation trail.
8. Brother Connect PDFs: candidate relationship-network predecessor to UserPath.

These anchors were supplied by an earlier thread and still require a local, hashed source manifest before they can be treated as verified extraction.

## Boundary rules

- This capsule is read-only candidate evidence.
- It does not promote UserPath to final canon.
- It does not import Chris private memory.
- It does not import private thread archives beyond the submitted summary.
- It preserves uncertainty where source artifacts have not been hashed and parsed.
- Extend it only through manifest-backed sources with hashes and local receipts.

## Next local build

Create `data/userpath/source_manifest.jsonl` with one row per source artifact:

- source_path
- source_label
- source_kind
- claim_type
- sensitivity_level
- sha256, when locally available
- canon_status
- promotion_status

Pass condition: ORACLE can explain UserPath from grounded evidence without inventing missing links.
