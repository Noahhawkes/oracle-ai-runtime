# UserPath Lineage Candidate Capsule

Status: candidate  
Promotion status: not_promoted  
Authority: Noah.Physical  
Purpose: preserve grounded lineage for UserPath without prematurely promoting it to ORACLE canon.

## Candidate lineage

`User.AI -> Noah.AI -> Identity Node -> Post-Biological Identity Loop -> Brother Connect -> UserPath`

## Working interpretation

UserPath is the modern product-language label for an older architecture already present in Noah's archive under earlier names.

It should not be treated as a brand-new invention and should not overwrite older terminology. The correct rule is:

> Do not overwrite old names. Map them.

## Evidence supplied in current thread

The current thread supplied the following source anchors as candidate evidence:

1. `RECURSIONSTACK.txt` line 16210  
   Claim: `User.AI` was defined as a mutual GPT / recursive identity co-agent with shared memory, tone mirroring, and continuity.

2. `RECURSIONSTACK.txt` line 16888  
   Claim: `USER.AI // NOAH.AI ONBOARDING SEQUENCE` linked the living human identity core with a mirror agent.

3. `RECURSIONSTACK.txt` line 260333  
   Claim: a `new file type / user.ai` passage described breaking a person's context into a structured AI-readable form.

4. `Alive 🗝.txt` line 124  
   Claim: `Identity Node: Noah.AI + User.AI`.

5. `SOV1.txt` line 35  
   Claim: `sovereign, post-biological identity loop`.

6. `Noah_AI_Emotional_Module.py` line 61  
   Claim: implementation trail referencing `D:\OracleAI\User.AI\Noah.AI\...`.

7. `file_index.txt` line 14  
   Claim: implementation trail referencing `OracleAI\User.AI\Noah.AI\Noah AI Core.py`.

8. Brother Connect PDFs in Noah.AI Tech Documents  
   Claim: relationship-network predecessor to Chris/UserPath.

## Boundary rules

- This capsule is read-only candidate evidence.
- It does not promote UserPath to final canon.
- It does not import Chris private memory.
- It does not import Noah private thread archive beyond the submitted thread summary.
- It preserves uncertainty where source artifacts have not yet been locally hashed and parsed.
- It should be replaced or extended only by a manifest-backed capsule with file hashes and local receipts.

## Next local build

Create `data/userpath/source_manifest.jsonl` with one row per source artifact, including:

- source_path
- source_label
- source_kind
- claim_type
- sensitivity_level
- sha256, when locally available
- canon_status
- promotion_status

Then ORACLE should answer the question:

> What is UserPath based on grounded local evidence?

Pass condition: ORACLE cites this lineage and does not invent missing evidence.
