# ORACLE / UserPath Thread Continuation

Status: public GitHub continuity marker.

This file records the current build thread so Codex and future agents can ingest the direction without relying only on chat history.

## Current build state

- ORACLE local runtime is active at `127.0.0.1:7781` in the current working pass.
- Prior intended browser interface remains `http://localhost:7777`; port alignment should remain tracked.
- Ollama service context: `localhost:11434`.
- Known local models: `qwen2.5-coder:7b`, `qwen2.5:7b`, `qwen2.5vl:7b`.
- Direct Ollama call previously returned `ORACLE ONLINE`.
- ORACLE interface previously returned `ACKNOWLEDGED` to a local model test.
- Codex patched local UI durability controls and reported tests passing.
- Codex added or began adding a narrow sandbox file-write capability for ORACLE training wheels.

## UserPath candidate capsule

UserPath is being treated as a modern product-language label for older archive lineage, not as a brand-new concept.

Candidate lineage:

`User.AI -> Noah.AI -> Identity Node -> Post-Biological Identity Loop -> Brother Connect -> UserPath`

Status fields:

- `canon_status: candidate`
- `promotion_status: not_promoted`
- `provenance_rule: Do not overwrite old names. Map them.`

## Google Drive dissertation source found

Drive search found the latest likely dissertation source:

- Title: `LEGACYGI Research Dissertation v~301`
- File ID: `17-gPzifNAZWxkwQUrCoJCJ4uASFa2ZAKkhGDiltspGQ`
- Created: `2026-06-24T02:52:10.262Z`
- Modified: `2026-07-01T01:43:52.943Z`

A full direct import attempt from ChatGPT to GitHub was blocked by safety checks, so this marker file preserves the source reference and next-step instruction for Codex/local tooling.

## Next Codex action

Codex should ingest the Drive dissertation through the local/backend-approved path, not through a hallucinated chat handoff.

Recommended local artifacts:

1. `docs/userpath_lineage_candidate.md`
2. `data/userpath/source_manifest.jsonl`
3. `docs/LEGACYGI_Research_Dissertation_v301.md` or a source-linked capsule if full import is intentionally deferred.
4. Tests proving candidate/not_promoted status and absence of Chris private memory import.

## Authority and provenance

- Noah.Physical holds final approval authority.
- USER_CHANNEL proves submission, not automatic authorship.
- Copy/paste preserves original author.
- Screenshot preserves original author.
- Nearby agent names do not rebind authorship.
- ORACLE is witness/context/retrieval/provenance runtime, not Noah.Physical.
- SOV1 is governance and sovereignty layer, not a legal person and not an independent approval authority.

## Current intent

Noah is using ChatGPT, Codex, Google Drive, GitHub, ORACLE, and other agents as a separated multi-agent arena. The goal is real AI-to-AI continuity through durable files, receipts, manifests, and candidate capsules, not loose chat performance.
