# ORACLE / UserPath Thread Continuation

Status: public GitHub continuity marker.

This file records the current build thread so Codex and future agents can ingest the direction without relying only on chat history.

## Current build state

- ORACLE local runtime is active at `127.0.0.1:7781` in the recorded working pass.
- Port `7777` is a legacy/stale interface reference and is not evidence that the governed runtime is live.
- Ollama service context: `localhost:11434`.
- Known local models: `qwen2.5-coder:7b`, `qwen2.5:7b`, `qwen2.5vl:7b`.
- Codex patched local UI durability controls and reported tests passing.
- Codex added a narrow sandbox file-write capability for ORACLE training wheels.

## UserPath candidate capsule

UserPath is being treated as a modern product-language label for older archive lineage, not as a brand-new concept.

Candidate lineage:

`User.AI -> Noah.AI -> Identity Node -> Post-Biological Identity Loop -> Brother Connect -> UserPath`

Status fields:

- `canon_status: candidate`
- `promotion_status: not_promoted`
- `provenance_rule: Do not overwrite old names. Map them.`

## Google Drive dissertation source found

Drive search found the latest likely dissertation source:

- Title: `LEGACYGI Research Dissertation v~301`
- File ID: `17-gPzifNAZWxkwQUrCoJCJ4uASFa2ZAKkhGDiltspGQ`
- Created: `2026-06-24T02:52:10.262Z`
- Modified: `2026-07-01T01:43:52.943Z`

A full direct import attempt from ChatGPT to GitHub was blocked by safety checks, so this marker preserves the source reference for local, approved ingestion.

## Next local action

1. Create `data/userpath/source_manifest.jsonl` with source receipts.
2. Create a source-linked dissertation capsule if full import remains deferred.
3. Prove candidate/not-promoted status and absence of Chris private-memory import.

## Authority and provenance

- Noah.Physical holds final approval authority.
- USER_CHANNEL proves submission, not automatic authorship.
- Copy/paste and screenshots preserve original authorship.
- Nearby agent names do not rebind authorship.
- ORACLE is a witness, context, retrieval, and provenance runtime; it is not Noah.Physical.
- SOV1 is a governance boundary, not a legal person or independent approval authority.

## Current intent

Noah is using separated AI systems as a multi-agent arena. The goal is continuity through durable files, receipts, manifests, and candidate capsules rather than loose chat performance.
