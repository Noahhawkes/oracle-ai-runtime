# Self-Build Proposal — 2026-06-08 20:01

**Target**: core/action_candidates.py
**Type**: missing_handler
**Risk**: low

## Title
Ensure action candidates are properly handled in SAFE_SLEEP mode

## Problem
In `core/action_candidates.py`, there is no specific handling for action candidates when SAFE_SLEEP_MODE is active.

## Solution
Add a check within the `_load()` and `_save()` methods to filter out or handle candidate actions differently when SAFE_SLEEP_MODE is active. For example, reject any new candidates during SAFE_SLEEP_MODE and only allow pending candidates to be loaded.

## Test
Modify `core/resident_runtime.py` to enter SAFE_SLEEP_MODE temporarily and verify that no new action candidates are created or saved while in this mode.

---

## Raw LLM Output
```
TARGET_FILE: core/action_candidates.py
CHANGE_TYPE: missing_handler
TITLE: Ensure action candidates are properly handled in SAFE_SLEEP mode
PROBLEM: In `core/action_candidates.py`, there is no specific handling for action candidates when SAFE_SLEEP_MODE is active.
SOLUTION: Add a check within the `_load()` and `_save()` methods to filter out or handle candidate actions differently when SAFE_SLEEP_MODE is active. For example, reject any new candidates during SAFE_SLEEP_MODE and only allow pending candidates to be loaded.
RISK: low
TEST: Modify `core/resident_runtime.py` to enter SAFE_SLEEP_MODE temporarily and verify that no new action candidates are created or saved while in this mode.

```python
# core/action_candidates.py

def _load():
    if get_governance().get('SAFE_SLEEP_MODE', False):
        # Filter out any new candidate actions during SAFE_SLEEP_MODE
        return [candidate for candidate in load_candidates() if candidate['status'] == 'pending']
    else:
        return load_candidates()

def _save(candidates):
    if get_governance().get('SAFE_SLEEP_MODE', False):
        # Only allow pending candidates to be saved during SAFE_SLEEP_MODE
        candidates = [c for c in candidates if c['status'] == 'pending']
    save_candidates(candidates)
```

This change ensures that ORACLE does not propose or execute new actions when in a state where she should be conserving resources, thereby reducing unnecessary activity and potential risks.
```
