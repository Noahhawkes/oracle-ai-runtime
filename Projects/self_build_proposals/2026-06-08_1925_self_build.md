# Self-Build Proposal — 2026-06-08 19:25

**Target**: core/action_candidates.py
**Type**: enhancement
**Risk**: low

## Title
Ensure pending action candidates are cleared after approval or rejection

## Problem
Pending action candidates remain in memory indefinitely, which can lead to clutter and potential misuse.

## Solution
Add a mechanism to clear pending action candidates after they have been approved, rejected, quarantined, or revoked. This can be done by implementing a cleanup function that is called whenever the state of a candidate changes.

## Test
Verify that pending candidates are removed from memory after being processed (approved, rejected, etc.) by checking the count of candidates in the list_candidates method before and after an approval or rejection action.

---

## Raw LLM Output
```
TARGET_FILE: core/action_candidates.py
CHANGE_TYPE: enhancement
TITLE: Ensure pending action candidates are cleared after approval or rejection
PROBLEM: Pending action candidates remain in memory indefinitely, which can lead to clutter and potential misuse.
SOLUTION: Add a mechanism to clear pending action candidates after they have been approved, rejected, quarantined, or revoked. This can be done by implementing a cleanup function that is called whenever the state of a candidate changes.
RISK: low
TEST: Verify that pending candidates are removed from memory after being processed (approved, rejected, etc.) by checking the count of candidates in the list_candidates method before and after an approval or rejection action.
```
