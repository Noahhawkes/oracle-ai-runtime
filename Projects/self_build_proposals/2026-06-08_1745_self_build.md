# Self-Build Proposal — 2026-06-08 17:45

**Target**: core/action_candidates.py
**Type**: missing_handler
**Risk**: low

## Title
Ensure all action candidates are properly handled after rejection or revocation.

## Problem
Currently, rejected, revoked, or quarantined action candidates do not receive any specific handling post-rejection. This can lead to confusion and potential misuse if these candidates are not explicitly marked as inactive or removed from active consideration.

## Solution
Add a mechanism to mark and handle rejected, revoked, or quarantined action candidates by updating the `ProjectState` module to include a status field for each candidate and ensure that such candidates are not considered in future approval cycles. Implement logic in `action_candidates.py` to filter out these candidates before they reach the actuation engine.

## Test
Verify that rejected, revoked, or quarantined action candidates are no longer listed when querying active candidates and do not appear in any subsequent approval cycles.

---

## Raw LLM Output
```
TARGET_FILE: core/action_candidates.py
CHANGE_TYPE: missing_handler
TITLE: Ensure all action candidates are properly handled after rejection or revocation.
PROBLEM: Currently, rejected, revoked, or quarantined action candidates do not receive any specific handling post-rejection. This can lead to confusion and potential misuse if these candidates are not explicitly marked as inactive or removed from active consideration.
SOLUTION: Add a mechanism to mark and handle rejected, revoked, or quarantined action candidates by updating the `ProjectState` module to include a status field for each candidate and ensure that such candidates are not considered in future approval cycles. Implement logic in `action_candidates.py` to filter out these candidates before they reach the actuation engine.
RISK: low
TEST: Verify that rejected, revoked, or quarantined action candidates are no longer listed when querying active candidates and do not appear in any subsequent approval cycles.
```
